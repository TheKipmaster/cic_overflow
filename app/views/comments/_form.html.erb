<%= form_for [commentable, Comment.new] do |f| %>
  <div class="field">
    <%= f.label :comentar %>
    <%= f.text_area :body %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>